name: CI-Dictionary-unit-test

on:
  push:
    branches:
      - main
      - dev2
      - v1

jobs:
  my-testing:
    runs-on: ubuntu-latest
    
    steps:
        - run: echo "Hello World"
        - run: ping www.google.com -c 10
        - run: ls -la
